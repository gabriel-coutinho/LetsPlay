const DATE_FORMAT = 'DD/MM/YYYY';
const FIVE_MB = 5 * 1024 * 1024;
const STATUS = {
  OPEN: 'OPEN',
  EXPIRED: 'EXPIRED',
  ACCEPTED: 'ACCEPTED',
  REJECTED: 'REJECTED',
  FULL: 'FULL',
};

module.exports = {
  DATE_FORMAT,
  FIVE_MB,
  STATUS,
};
